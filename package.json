{
  "name": "dukandost-pro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install-all": "npm install --include=dev && cd Backend && npm install --include=dev && cd ../Frontend && npm install --include=dev",
    "build": "cd Frontend && npm install --include=dev && npm run build && cd .. && node build.js",
    "start": "cd Backend && NODE_ENV=production npm start",
    "dev": "concurrently \"npm run dev-server\" \"npm run dev-client\"",
    "dev-server": "cd Backend && npm run dev",
    "dev-client": "cd Frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "tsx": "^4.21.0"
  }
}
